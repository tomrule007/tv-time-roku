' Default config used when the app is run directly from source.
' scripts/build.js generates a configured version in dist/build.
function GetConfiguredBackendAddress() as string
    return ""
end function
